# 242. Revisiting the "Rules of Hooks" and Why to use Hooks

-   Only call hooks inside of component functions or other custom hooks
-   Only call hooks on the top level
    -   React hooks must not be called in nested code statements
        -   Ex. If Statements
-   Custom hooks are used to wrap and reuse code in your component functions

# 243. Creating a Custom Hook

-   Best practice to toss it in a hooks folder
-   Hooks should have the keyword 'use' before defining the function

# 245. Exposing nested Functions from the Custom Hook

-
